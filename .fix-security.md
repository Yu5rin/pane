# セキュリティ修正メモ（作業ログ・随時追記）

作業ディレクトリ: /workspace/pane、ブランチ claude/pane-phase-1-setup-87833g、開始HEAD 66d7bc8。
`.review-security.md` の総括「今すぐ直すべきもの」1〜4のうち、セキュリティ/プライバシーに関わる次の4件を直す。
（5番の文言不整合はスコープ外。今回は触らない。）

対象:
1. Pandoc をパス無しで起動している（最優先）
2. ログに検索語が平文で残る
3. ログに文書の絶対パスが大量に出る（+ ExternalLinkServiceのURL）
4. 文書を開いただけで外部へリクエストが飛ぶ（referrerポリシー等）

## 進捗

- [x] 1. Pandoc PATH解決
- [x] 2. 検索語のログ
- [x] 3. パスのログ方針(方針a: 既定は短く、詳細ログでフル)
- [x] 4. 外部リソース自動読み込み（referrer-policy等。設定項目の追加はせず、metaタグ+文書修正に留めた）

---

## 1. Pandoc をパス無しで起動していた件（完了）

裏取り: 修正前のコードを実際に読んで確認した。
- `Pane/SettingsBridge.cs:899`(修正前) `Process.Start(new ProcessStartInfo("pandoc", "--version") {...})` — 拡張子・パス無し。
- `Pane/MainForm.cs`(ExportViaPandocAsync内、修正前) `new ProcessStartInfo("pandoc") {...}` — 同上。
- 呼び出し経路: `MainForm.PostCapabilities`(3437行付近) → `SettingsBridge.DetectPandocAvailable()`(3545行付近)。
  `PostCapabilities` は起動直後のメッセージ受信(1380行)と設定変更後の再配信(3416行)から呼ばれる。
  つまりエクスポート操作をしなくても起動時に呼ばれる、という指摘どおりだった。
- Win32のCreateProcessは拡張子無しのファイル名に自動で".exe"を補い、
  (1)実行ファイル(Pane.exe)のあるフォルダ→(2)カレントディレクトリ→(3)System32等→(4)PATH の順で探す。
  Paneはポータブル配布でダウンロードフォルダに置かれがちなので、指摘は妥当と判断した。

直した内容:
- 新規 `Pane/ExternalToolLocator.cs`(`internal static class ExternalToolLocator`)を追加。
  `ResolveFromPath(exeBaseName, pathEnvironmentVariable, fileExists)` がPATH環境変数のフォルダだけを
  順に走査し、"<exeBaseName>.exe"が実在する最初の絶対パスを返す(無ければnull)。ファイルシステムには
  一切触れない(fileExistsを引数で受け取る)ので、Windows専用APIに依存しない。空エントリ・"."(カレント
  ディレクトリを指す)は明示的にスキップする。
- `Pane/SettingsBridge.cs DetectPandocAvailable()`: `ExternalToolLocator.ResolveFromPath("pandoc", Environment.GetEnvironmentVariable("PATH"), File.Exists)`
  で得た絶対パスがあるときだけ`Process.Start`する。見つからなければ従来どおり「Pandocが無い」扱い。
- `Pane/MainForm.cs ExportViaPandocAsync(...)`: 同様にPATHから絶対パスを解決し、見つからなければ
  `InvalidOperationException("Pandocが見つかりませんでした。")`を投げる(旧コードはWin32Exceptionを
  そのまま伝播させていたのを、意図の分かる例外メッセージに変えた)。

なぜ起きたかのコメント: `Pane/ExternalToolLocator.cs`のクラスコメントに、CreateProcessの検索順・
ポータブル配布ゆえの実害・PostCapabilitiesからも呼ばれる起動時実行の経路を明記した。

追加したテスト: `Pane.Tests/ExternalToolLocatorTests.cs`(8件)。`Pane.Tests/Pane.Tests.csproj`の
`<Compile Include>`に`../Pane/ExternalToolLocator.cs`を追加。
- PATH上のフォルダにあれば見つかる
- 拡張子を書かなくても.exeを補って探す
- PATHの複数フォルダを先頭から順に探す
- PATHのどこにも無ければnull
- PATH環境変数が空またはnullならnull
- カレントディレクトリを意味する"."はPATHに入っていても見ない
- 空エントリはスキップしてカレントディレクトリを見ない
- 実行ファイルのフォルダやカレントディレクトリは引数として渡さない限り検索対象にならない(見た候補を記録して検証)

テストが修正前に落ちることの確認: `ExternalToolLocator`クラス自体が今回新規追加のため、
修正前のコードにはこのクラスが存在せず、テストはそもそもコンパイルできない(=当然失敗する)。
「テストが無かったこと自体が問題」というLargeFileGuardと同型のケース。ロジック抽出後の
`dotnet test`(94件、うち新規8件)は全件成功、`dotnet build ... Release`も成功を確認済み。

念のためexe抽出後、修正前(git stashで一時的に戻す等)の再現実験はしていない。
`Process.Start(new ProcessStartInfo("pandoc", ...))`という文字列そのものを直接読んで
確認したので、Windows実機無しでも挙動は文書化されたWin32仕様から確実に言える。

---

## 2. ログに検索語が平文で残る件（完了）

裏取り: 修正前のコードを実際に読んで確認した。
- `Pane/MainForm.cs`(HandleGlobalSearchRequest内) `Logger.Write($"global-search開始: root=..., text=\"{queryText}\", ...")`。
- `Pane/SearchService.cs`(SearchAsync内) `Logger.Write($"...: text=\"{query.Text}\", regexp=...")`。
  いずれもInfo(既定)で記録される。queryTextはJSから届いたグローバル検索ボックスの入力そのもの。

直した内容:
- `Pane/MainForm.cs`: `text="{queryText}"` を `textLength={queryText.Length}` に変更(検索語そのものは書かない)。
- `Pane/SearchService.cs`: 同様に `text="{query.Text}"` を `textLength={query.Text.Length}` に変更。
- どちらもコメントで「検索語そのものはログへ書かない。ログ送付が運用に組み込まれているため」の理由を残した。

これ以外に検索語を書いているログ箇所は無いことを`query.Text`/`queryText`でgrepして確認した
(SearchService.cs内のBuildRegex/SearchInFile等は文字数・件数のみで元々問題なし)。

テストが修正前に落ちることの確認: これは文字列フォーマットの変更であり、既存のテストで
検索語ログの有無を検証していたものは無い(新規にテストを追加する類の判断ロジックでもない
ため、テストは追加していない)。目視での確認に留まる。

---

## 3. ログに文書の絶対パスが大量に出る件（完了・方針(a)を選択）

### 方針の選択: (a) 既定は短く、詳細ログ(Debug)でのみ完全なパス

選んだ理由:
- (b)(取扱説明書に明記するだけ)は「送る前に判断できる」という受け身の対策で、実際には
  利用者が読み飛ばしてそのまま送ってしまう可能性が残る。一方(a)は既定の動作そのものを
  安全側に倒すので、利用者が何も意識しなくても既定ログの露出が減る。
- CLAUDE.mdの方針「外の世界に触れない判断ロジックは依存の無いクラスへ切り出してテストで
  固定する」と相性が良い(実際に`PrivacyLogFormatter`として切り出せた)。
- 「不具合調査にはパスが要る場面もある」という懸念には、既存の「詳細ログを記録する」設定
  (Logger.Debug、既に実装済みでPaneに元々ある仕組み)をそのまま使うことで応えられる。
  新しい設定項目を増やさずに済む。
- 加えて、(a)だけでは「詳細ログを有効にして送る」場合にフルパスが載ることに変わりはないため、
  取扱説明書にも軽く一言(ファイル名は既定でも載ること、詳細ログではフルパスが載ること)を
  添えた(完全に(b)をやったわけではなく、(a)を主軸にした上での最小限の補足)。

裏取り: 修正前のコードを実際に読んで確認した(MainForm.cs OpenFile/保存成功/OpenInNewTab/
LoadFolderAsync、FolderService.cs open-in-default-app/reveal-in-explorer/delete-path/
rename-path/create-file-in-folder、Program.cs起動args、PaneApplicationContext.csの起動時
フォルダ・OpenWindow・preload等、ExternalLinkService.csのnew-window-requested)。いずれも
Windowsのユーザーフォルダを含むフルパス、またはクエリ文字列付きのURLがInfo(既定)で
そのまま記録されていた。

直した内容:
- 新規 `Pane/PrivacyLogFormatter.cs`(`internal static class PrivacyLogFormatter`)を追加。
  - `ShortenPath(path)`: パス区切り("\"と"/")の最後の要素だけを返す(ファイル名/フォルダ名)。
    `Path.GetFileName`を使わなかった理由: LinuxでのOS既定の区切り判定では"\"を区切りと
    認識せず、CIの`dotnet test`はubuntu-latestで走る(.github/workflows/ci.yml)ため、
    Windowsパスを正しく分割できずテストが誤って通ってしまう。区切り文字を自前で
    `LastIndexOfAny(['\\','/'])`判定することで、OS非依存にした。
  - `ShortenUri(uri)`: スキームとホスト(+ポート)だけを残し、パス・クエリ文字列・
    フラグメントを落とす。
  - `ShortenPathOrUri(value)`: `FolderService.OpenInDefaultApp`のようにパスとURLの両方を
    受け取りうる箇所向けの振り分け。
  - 例外(Logger.WriteException、Errorレベル)は対象外にした。頻度が低く「大量に出る」
    問題には当たらず、失敗の原因調査にはむしろ完全なパスが要ることが多いため。
- 適用箇所(すべて「Write(短縮形)」+「Debug(完全な形)」の2行にした。詳細ログを
  有効にしている利用者だけが完全な形を得られる):
  - `Pane/MainForm.cs`: 保存成功、OpenFile、OpenInNewTab、LoadFolderAsync開始/完了/キャンセル
  - `Pane/FolderService.cs`: open-in-default-app、reveal-in-explorer、delete-path、
    rename-path(移動元・移動先の両方)、create-file-in-folder
  - `Pane/Program.cs`: 起動時のargs(コマンドライン引数=開いたファイルのフルパス)
  - `Pane/PaneApplicationContext.cs`: 復元確認スキップ時のOriginalPath、起動時カスタム
    フォルダ、OpenWindow(フォルダ起動)、既存ウィンドウへのタブ追加計測ログ、preload受信
  - `Pane/ExternalLinkService.cs`: new-window-requestedのURL(ShortenUriを適用)
- `docs/取扱説明書.md`(不具合の調査に協力する節): 既定ログにもファイル名は残ること、
  「詳細ログを記録する」を有効にするとフルパスが載ることを一文ずつ追記した。

なぜ起きたかのコメント: `Pane/PrivacyLogFormatter.cs`のクラスコメントに、ログ送付の運用実態
(3日分が共有された実績)と、既定/詳細ログの使い分けを選んだ理由を明記した。

意図的に対象外にしたもの(スコープを絞った理由): `pane-file.local`アクセス拒否ログ
(MainForm.cs、範囲外アクセスの拒否記録。セキュリティ上むしろフルパスが有用)、
カスタムCSSパス・画像挿入パス・テーマフォルダ・設定/ログファイル自体のパス
(いずれも指摘元の一覧に無く、[低]評価だった項目)には手を入れていない。

追加したテスト: `Pane.Tests/PrivacyLogFormatterTests.cs`(9件)。`Pane.Tests/Pane.Tests.csproj`
の`<Compile Include>`に`../Pane/PrivacyLogFormatter.cs`を追加。
- Windowsのフルパスからファイル名だけを残す
- フォルダパスは末尾のフォルダ名だけを残す
- null/空文字はそのまま返す
- ドライブのルートはそのまま返す
- URLはスキームとホストだけを残しクエリ文字列を落とす
- URLのポートは残す
- 絶対URLとして解析できない文字列はそのまま返す
- ShortenPathOrUriはURLをURLとして短くする
- ShortenPathOrUriはファイルパスをパスとして短くする

テストが修正前に落ちることの確認: `PrivacyLogFormatter`クラス自体が新規追加のため、
修正前のコードにはこのクラスが存在せずテストはコンパイルできない(ExternalToolLocatorと同型)。
なお実装中に、`Path.GetFileName`をそのまま使う版で一度実装したところ、Linux上の
`dotnet test`ではWindowsパスの"\"が区切りとして認識されず、
`Windowsのフルパスからファイル名だけを残す`のテストが実際に**失敗する**ことを確認した
(意図せず「テストが修正前に落ちる」を別の形で体験した形)。区切り文字を自前判定する版に
直してから全件成功を確認した。



---

## 4. 文書を開いただけで外部へリクエストが飛ぶ件（完了・meta タグ + 文書修正のみに留めた）

裏取り: 修正前のコードを実際に読んで確認した。
- `src/editor.js resolveImageSrc`(254行付近): `http(s)://`のsrcはそのまま返す(未加工でimg.srcへ入る)。
- `src/editor.js ImageWidget.toDOM`(576-578行付近): 上記srcをそのまま`img.src`に設定する。
- `src/html-sanitize.js`(22行付近 ALLOWED_TAGS): `iframe`を許可タグに含む。
- `src/html-sanitize.js`(207-213行付近): 属性値のURLはjavascript:/vbscript:/data:(画像以外)
  以外を素通しする(http/httpsはそのまま残る)。
- `src/index.html`(修正前): `<meta name="referrer">`もCSPも無かった(grep 0件で確認)。
  → 文書を開いてその行が画面に入った時点で、`https://`画像・iframeのURLへ自動でリクエストが
  飛び、既定のRefererとして`https://pane.local/`が付く、という指摘は事実だった。

方針の選択: 最低限のmetaタグ + 文書(README・取扱説明書・仕様書)の記述修正に留め、
外部リソースの自動読み込みを止める設定項目は**追加しなかった**(以下「提案」として記載)。

理由:
- 判断に迷った場合はメタタグと文書修正に留める、という指示に従った。
- 自動読み込みを既定オフにする設定は、UIの追加(設定「画像」節への項目追加)・
  editor.js/html-sanitize.js側の読み込み許可判定の分岐・「今回だけ読み込む」操作の追加など
  実装面が広がり、Windows実機で確認できない状態で仕様書 第9章の実装フェーズの先取りに
  なりかねないと判断した。CLAUDE.md「実装フェーズの順序を守る。現在のフェーズを超える
  機能を先取りしない」にも抵触しうる。
- 一方で、`<meta name="referrer" content="no-referrer">`はブラウザ標準機能で、副作用が
  無く(既存のCSSプロパティ等に影響しない)、参照先へ渡る情報を一つ確実に減らせるため、
  最低限の対策として追加した。

直した内容:
- `src/index.html`・`src/settings-window.html`・`src/help-window.html` の`<head>`先頭付近に
  `<meta name="referrer" content="no-referrer">`を追加(理由をHTMLコメントで明記)。
  index.htmlが本題(文書表示)だが、設定/取扱説明書ウィンドウも外部リンクを開く経路がある
  ため、同じ理由で3ファイルとも揃えた。
- `README.md`: 「外部と通信するのは更新の確認だけ」の一文を「**自分から**外部へ問い合わせに
  行くのは更新の確認だけ」に直し、文書中の`https://`画像・埋め込みが表示のため通信を伴う旨を
  括弧書きで追記。
- `docs/取扱説明書.md`:
  - 冒頭(1章)の「外部と通信するのは新しい版があるかを尋ねるときだけ」の一文を同様に直し、
    文書中の外部画像・iframeが読み込み時に通信を伴う旨と、Refererヘッダーを送らない設定に
    したうえでの限界(参照先URL自体は文書の作者が把握できる=トラッキングピクセルを完全には
    防げない)を1段落追加。
  - 「外部通信について」節(第8章)に同内容の段落を追加。
  - 「不具合の調査に協力する」節に、既定ログにもファイル名が残ること・詳細ログでフルパスが
    残ることを追記(3番の修正と共通)。
- `docs/仕様書.md`は今回変更していない(判断・機能の記述自体は変えず、実装済みの挙動の
  説明をREADME・取扱説明書側で補う方針にした)。

なぜ起きたかのコメント: `src/index.html`のmetaタグ直前にHTMLコメントで、Refererに
`https://pane.local/`が付くこと・.review-security.md C-5を参照して理由を明記した。

ビルド確認: `npm run build`後、`dist/index.html` `dist/settings-window.html`
`dist/help-window.html`いずれにも`<meta name="referrer" content="no-referrer">`が
出力されることを確認した。

テストについて: これはHTML/文書のみの変更で、判断ロジック(依存の無いクラスへ切り出す対象)
が無いため、新規テストは追加していない。回帰スイート(`npm run verify`)で、既存のライブ
プレビュー・HTMLサニタイズ関連のテストが壊れていないことを確認する(後述)。

### 提案(実装していない。判断に迷ったため報告に留める)

外部リソースの自動読み込みそのものを既定オフにし、次のような形にすることを提案する
(仕様書 第9章の実装フェーズ・設定画面の設計を伴うため、別途の判断・作業として切り出すべき):
- 設定「画像」節あたりに「文書中の外部画像・埋め込みを自動で読み込む」チェック(既定ON/OFFは
  要検討)を追加する。
- OFFのときは`src/editor.js`のImageWidget/html-sanitize.jsのiframe許可判定で、`http(s)://`の
  読み込みをプレースホルダ表示に差し替え、クリックした/バーで明示操作したときだけ読み込む
  ようにする。
- README・取扱説明書の該当箇所をこの新しい既定に合わせて書き直す。

---

## 検証結果(最終・すべて完了後)

```
npm run build          -> 成功(エラー無し)
npm run verify          -> 60本 / OK=2451 NG=0 (全スイートNG 0件)
dotnet test              -> Passed: 103, Failed: 0, Skipped: 0 (既存86 + 新規17)
dotnet build Pane/Pane.csproj -c Release -p:EnableWindowsTargeting=true -> Build succeeded (Warning 1件、既存のWindowsBaseバージョン競合警告のみ。今回の変更とは無関係)
```

verify実行後、静的サーバー等の残プロセスが無いことを確認済み。

4件すべて対応完了。コミット・プッシュはしていない(指示どおり)。
